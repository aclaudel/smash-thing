export default class GameEngine {

    moveCharacter(id: string) {
        throw new Error();
    }

    left(id: string) {
        throw new Error();
    }

    right(id: string) {
        throw new Error();
    }
}